const categoricDefaultConfig = {
  isSync: true,
  chartType: "stacked",
  stackedMode: "total",
  showLegend: true,
  showGrid: true,
  groupOrder: "alpha",
  categoryOrder: "alpha",
  axisLabelFontSize: 16,
};

export default categoricDefaultConfig;
